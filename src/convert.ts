export class Convert {
  // Cette méthode doit être complétée pour convertir un nombre entier en chiffres romains.
  // Vous devez écrire le code nécessaire pour faire passer les tests.
  toRoman(num: number): string {
    let roman = '';
    roman = 'I';

    // Votre code ici

    return roman;
  }
}
